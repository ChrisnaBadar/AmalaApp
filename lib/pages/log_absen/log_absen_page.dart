import 'package:flutter/material.dart';

class LogAbsenPage extends StatelessWidget {
  const LogAbsenPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
